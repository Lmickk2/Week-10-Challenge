const employee = require("./members/employee");

describe('Employee', ()=>{
    it('Should prompt the user to input name',()=>{
        expect(`${response.name}`)
    })
        

    it('Should prompt the user to input email',()=>{
        expect(`${response.email}`)
    })

    it('Should prompt the user to input id',()=>{
        expect(`${response.id}`)
    })
})